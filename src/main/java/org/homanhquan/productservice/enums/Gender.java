package org.homanhquan.productservice.enums;

/**
 * Enum (short for enumeration) in Java is a special data type that defines a set of fixed, constant values.
 * It makes code more readable, type-safe, and easier to maintain compared to using plain constants.
 */
public enum Gender {
    MALE,
    FEMALE
}
